# S10R4 Binding-04 Resolver-Effective Recovery Authority (A51)

Status: user-approved higher-order scientific amendment, pending exact
precommit review and postcommit audit.  No outcome evidence, effective lock,
formal report or outgoing edge is created by this document.

## 1. Trigger and direct evidence

Binding-03 implemented the A50 singleton backend repair
`Backend.Name: Exhaustive -> Tensile` and completed fresh contract validation,
prelabel source materialization, complete Tensile subtree verification, native
build and replay verification.  Its required nonformal diagnostic then reached
exactly one resolved `Solution` and the deliberate pre-KernelWriter sentinel,
but rejected literal raw/state equality.

The immutable row-0 investigational projection records:

| Raw declaration | Resolver-effective observation | Classification |
| --- | --- | --- |
| `MatrixInstruction=[32,32,8,1,1,1,7,4,1]` | `[32,32,8,1]` plus exact reconstructing derived MI fields | reversible representation |
| `ScheduleGROverBarrier=1` | `0` | behavior-changing resolution |
| `StaggerU=16` | `0` | behavior-changing resolution |
| `StaggerUStride=256` | `0` | behavior-changing resolution |

The original benchmark write, KernelWriter, native execution, GPU and GFLOPS
were not reached.  This is not a KernelWriter failure, codegen attrition,
Ductile/Tensile builder disagreement or scientific terminal result.  It
falsifies the previous hidden assumption that raw candidate declarations are
always identical to the state consumed by the normal operational workflow.

Direct immutable records:

- field-diff result raw SHA-256
  `a8f9f49821490299a5da43862364308e86c60240fe70f09a4d3eb1da4c304b57`;
- independent Reviewer-A failure record raw SHA-256
  `f39c0639059c04bdbcb5c9eb0c5a8ac881f79fce0345ee358f32d7ad9c7356cd`;
- adversarial failure record raw SHA-256
  `a18b6ecfa02529255206fb4b188ec5692bc58a7f1b7ec030cd62b86d4b9c92ec`;
- approved design-discussion decision raw SHA-256
  `b94e7bb2b5c9ddba28311766e8d8e7178f9d8c7aad2c8ee07b3e134e8887b0ad`.

Two fresh `gpt-5.6-sol`/xhigh reviewers received the same prompt, completed one
cross-examination round and one evidence-backed final round, and both returned
`AGREE`.  At `2026-08-02T17:13:10Z`, the user stated
`在此輪實驗全部做完之前自動核准`, explicitly approving the pending binding-04
dual-lineage amendment and subsequent reviewer-supported in-scope decisions
through this experiment's completion.

## 2. Binding-03 prelock closure

At the A51 authority commit, binding-03 becomes:

- binding status: `superseded_prelock`;
- execution status: `cancelled`;
- checkpoint state: `BLOCKED`;
- criterion status: `CHANGES_REQUIRED`;
- scientific outcome: `not_evaluated`;
- edge: `null`;
- lock state: `absent`;
- lock never created: `true`;
- formal report, decision and gate record: forbidden.

Its run root, plans, prelabel build, diagnostic partial, command logs,
investigational record, implementation-byte identities and reviews remain
immutable diagnostic provenance.  They cannot be completed, overwritten,
renamed, copied into binding-04, counted as formal evidence or used for
binding-04 lock readiness.  S10R4 remains scientifically active only through
binding-04.

## 3. Binding-04 identity

The authorized successor is:

- binding ID: `binding-04`;
- execution tranche: `T-S10R4-B04`;
- closure unit: `CU-S10R4`;
- run root:
  `agent_run/260802-ductile-factorized-guidance-s10r4-effective-frame-binding-04`;
- scientific design:
  `study_docs/research/ductile-origami-warmstart/s10r4-stage1-resolver-effective-operational-entry-design.md`;
- machine contract:
  `study_docs/research/ductile-origami-warmstart/protocol/v1/s10r4-stage1-resolver-effective-entry-binding-04-contract.yaml`;
- lock:
  `study_docs/research/ductile-origami-warmstart/protocol/v1/locks/s10r4-stage1-resolver-effective-entry-binding-04-lock.json`;
- formal report remains the checkpoint-stable path
  `study_docs/research/ductile-origami-warmstart/reports/staged/s10r4-stage1-exact-frame-entry-report.md`.

The checkpoint ID, outcome criteria, failure IDs, correctness/noise thresholds
and only allowed positive edge remain S10R4-scoped.  The scientific population,
measurement boundary, selection unit, mapping provenance and positive claim are
amended exactly as follows.

## 4. Approved dual-lineage measurement boundary

### 4.1 Immutable raw population

The S10R3 G3 exact frame remains exactly 114 accepted occurrences and 114 raw
config hashes.  Every occurrence, raw byte, draw association and multiplicity
remains in the sampling population and all denominator statements.  Resolver
normalization, collision and KernelWriter attrition never remove raw occurrence
mass from this denominator.

### 4.2 Layered operational identities

Binding-04 must prelock a closed, canonical, source-pinned identity stack:

1. `declaration_identity`: raw occurrence, ForkParameters, raw bytes/hash and
   draw provenance;
2. `resolver_identity`: a semantic identity containing only the closed resolved
   and derived consumer fields, exact types, canonical bytes and frozen resolver
   schema/source identity; plus a separate evidence identity containing the raw
   declaration link, before/after transformation trace, guard operands and
   source rule/blob records;
3. `atom_identity`: effective realizations mechanically linked to the prelocked
   candidate universe;
4. `codegen_identity`: a semantic identity containing canonical
   `getKeyNoInternalArgs`, required assembly/helper semantic sets, normalized
   artifact semantics and post-filter membership class; plus a separate evidence
   identity containing raw occurrence/pass association, cwd/path, inventories,
   artifact digests and terminal records;
5. `mapping_identity`: `(operational_identity, size_id)` plus exact SizeMapping,
   Formocast/runtime inputs and required-field provenance.

Digest equality never substitutes for canonical-byte equality.  A closed
resolver schema must be derived from pinned source structure and synthetic
fixtures without inspecting the remaining 113 frame rows, independently
reviewed, committed and bound before the effective lock.  It must cover every
prelocked candidate axis plus all derived fields needed to distinguish
KernelWriter and mapping consumers.  A same-resolver projection producing
different codegen identity is projection falsification and
`CHANGES_REQUIRED`, not a new collision class.

MI representation is reversible only when the exact MI9 declaration can be
reconstructed from all required derived MI fields.  Every non-invertible
overwrite is `behavior_changing_resolution`, never equality.  Unknown fields,
unknown transformations, missing/type drift, source drift, ambiguous
canonicalization or unresolved size dependence fail closed.

`operational_identity` is exactly the canonical JSON object with the three
sorted-key members `resolver_semantic_identity`, `atom_identity` and
`codegen_semantic_identity`.  Canonical JSON is UTF-8, recursively key-sorted,
compact (no insignificant whitespace), finite-number-only and type preserving;
`operational_hash` is SHA-256 of those exact bytes.  Raw hash, occurrence/pass,
cwd, path, timestamps and evidence-file digests are forbidden from this product
but remain mandatory in the separate evidence identities.  Thus aliases can
collapse only when all consumer-relevant semantic bytes are equal, never merely
because a coarse digest or output status matches.

### 4.3 Atom and guidance semantics

Operational coverage counts only separately named effective atoms already
mechanically present in the prelocked candidate universe.  A normalized-away
raw value supplies no raw-value, controllability or guidance witness.  Other
unchanged/effectively realized atoms in that raw occurrence may still witness
operational values.  Resolver-derived values outside the prelocked universe
cannot become post-hoc mandatory atoms.  A compound/grouped atom counts only
when its complete candidate tuple has an exact value-preserving or approved
reversible correspondence.

Effective realization and raw-intervention evidence are distinct fields.  An
effective value produced from another raw declaration may support the
effective-state entry fixture, but never proves that its matching raw value was
sampled, controllable or guidance-eligible.

## 5. Formal workload and collision rules

No remaining-row resolver audit is allowed before the binding-04 lock.
Source-only static analysis, synthetic fixtures and known row-0 regression are
prelock and nonformal.  After `LOCKED_READY`, each formal census child records
its raw-to-resolver projection at the normal pre-KernelWriter boundary and then
continues through the pinned original write/KernelWriter path.

The workload remains complete and fixed:

- Census A: ordered registry indices 0–113;
- Census B: ordered registry indices 0–113, only after A is complete;
- no early stop, reordering, deduplication, replacement, third pass or prefix
  credit from earlier bindings;
- all 228 terminal children retain raw occurrence identity even when their
  effective states collide.

Resolver projection must be deterministic across A/B for every occurrence.
First form a resolver-effective partition over all 114 raw occurrences; its
groups retain every raw alias and their multiplicities sum exactly to 114.  Each
partition group is then classified as a stable-survivor group or stable-attrition
group from all aliases and both passes.  Mixed success/attrition, different
terminal semantics, or different codegen semantic identities inside one
resolver-effective group is projection falsification and
`CHANGES_REQUIRED / not_evaluated`.  Stable survivor selection uses only the
stable-survivor subset; that subset's raw multiplicity equals survivor raw mass
and need not equal 114.  Alias collapse requires equality of the complete
resolver/atom/codegen semantic bytes, while every occurrence keeps its separate
evidence identity.

## 6. Selection, mapping and downstream gates

Selection units are unique stable operational identities, not raw aliases.
Mandatory atoms are the prelocked candidate atoms actually realized in stable
effective survivor states.  The deterministic greedy rule remains:

```text
C_greedy = deterministic realized-atom cover size
K = max(10, C_greedy)
K <= 20
```

Tie-break uses collision-group first raw registry occurrence, then raw hash;
padding uses the same order; final order is deterministic operational hash then
representative raw hash.  After complete evidence, fewer than ten unique stable
operational survivors, impossible realized-atom cover or `C_greedy>20` is the
existing mapping-negative branch.  It is not support absence.

Mapping A/B each remains exactly `3K` rows.  Every row binds the deterministic
raw representative, all aliases/multiplicity, resolver identity and trace,
codegen identity, size, SizeMapping, native Formocast/runtime inputs and field
provenance.  Mapping parity compares every relevant layer.  The decision table
is unique: guessed required field, consumer/projection mismatch, A/B identity
drift, partial or unknown is `CHANGES_REQUIRED`; first-attempt success is PASS
and forbids retry; only the exact same prelocked allowlisted native mapping
failure reproduced in exactly two complete attempts is mapping-negative;
failure→success, different signatures or any third attempt is
`CHANGES_REQUIRED`.  Native conformance, three anchors × three sizes correctness
and 63-cell noise workload/thresholds remain unchanged.

## 7. S11 lineage and claim boundary

S11 must use fresh multiplicity-preserving:

```text
F_valid(raw) -> F_effective -> F_codegen
```

Raw accepted-occurrence mass remains the denominator.  Mapping and operational
features consume effective semantics.  A raw value receives guidance evidence
only where it is realized without behavior-changing overwrite under the frozen
relation.  Normalized-away, context-dependent or collision-confounded raw
values receive no raw-value guidance credit.  Whole-gene fail-closed eligibility
and deferred value-level guidance remain unchanged.

A positive S10R4 claim is limited to:

> This fixed 114-occurrence raw frame, through the pinned resolver and normal
> KernelWriter workflow, yields a reproducible bounded distinct-effective-state
> Stage-1 entry fixture.

It cannot claim literal raw realization, raw-to-effective injectivity,
value-level causal effects, general valid/operational support, survival
probability, ranking accuracy, factorization efficacy or production readiness.

## 8. Evidence reuse, implementation and seal boundary

Reusable only after fresh byte/ancestry validation:

- committed S10R4/S10R3 scientific authority and exact raw frame/provenance;
- actual YAML, source commits, prelocked candidate universe, sizes and native
  fixture;
- source-only semantic facts and pure helpers that read no earlier binding
  state;
- B02/B03 retirement and failure identities as governance provenance only.

Freshly required:

- binding-04 design/contract, resolver-schema record, run root and plans;
- complete source materialization/build and synthetic identity diagnostics;
- implementation, tests, prebinding audit, lock, activation and all 228 formal
  children;
- classification, collision/selection, mapping, native/GPU/correctness/noise,
  decision, reproduction, fresh verification and closeout.

Binding-03 prelabel products, diagnostics, plans, lock readiness conclusions,
current exact-seven intermediate bytes and any earlier mapping/outcome cannot
satisfy binding-04 gates.

The committed binding-04 machine contract, before Plan-B or Plan-A, must freeze
three exact and disjointly interpreted lists: implementation paths,
execution-artifact paths and delivery paths.  Its implementation list may draw
only from runner, schema, contract, frame, census, selector, mapping, state
machine, conformance/native/correctness adapters and the S10R4 unit test.
Plan-A only supplies decision-complete instructions with exact parity to those
contract lists; it cannot create or expand authority.  Neither contract nor
plan may silently expand to S11 implementation or another checkpoint.  The
Phase-2 seal must use exact-path staging, bind the new lock, preserve unrelated
dirty work and remain local.  No push is authorized.

## 8.1 Cumulative role-thread authority

S10R4 is one R3 gate/lineage; binding-04 does not reset its thread account.  The
pre-A51 account had five spawned role threads plus one reserved verifier slot:

1. `/root/pipeline_rebaseline_reviewer_a` — original design reviewer;
2. `/root/pipeline_rebaseline_reviewer_b` — original design reviewer;
3. `/root/s10r4_adversarial_auditor` — adversarial authority auditor;
4. `/root/s10r4_failure_reviewer_a` — independent operational reviewer;
5. `/root/s10r4_fresh_implementer` — gate implementer.

A51 used two additional fresh design reviewers,
`/root/s10r4_design_reviewer_a` and `/root/s10r4_design_reviewer_b`, bringing
the observed spawned total to seven.  The user's
`在此輪實驗全部做完之前自動核准` authority explicitly raises this S10R4
lineage's fresh-role cap from six to exactly eight: one and only one remaining
slot is reserved for the still-fresh terminal verifier.  Existing auditor,
reviewer and implementer roles must be resumed for all other B04 work; another
fresh implementer, auditor, design reviewer or replacement verifier is
forbidden without new explicit authority.  Repair count remains append-only and
uncapped, but neither binding nor new root resets role provenance.

## 9. Canonical outcomes and execution order

Canonical outcomes and priority remain:

1. `CHANGES_REQUIRED / not_evaluated / edge=null` for lineage, projection,
   implementation, partial, discordant or unknown evidence;
2. operational `BLOCKED / not_evaluated / edge=null` only for material external
   safety/capacity/preservation/authority boundaries;
3. `negative / S1_ENTRY_BLOCKED / FT-BLOCKED-MAPPING / edge=null`;
4. `negative / S1_ENTRY_BLOCKED / FT-BLOCKED-CORRECTNESS / edge=null`;
5. `inconclusive / FT-INCONCLUSIVE / edge=null` after complete noise evidence;
6. `positive / S1_ENTRY_GO_EXACT_FRAME / failure_id=null`.

Only a post-audited positive exposes
`S10R4:S1_ENTRY_GO_EXACT_FRAME -> S11`.

Required order is authority/design review and commit; machine contract review
and commit; fresh plans/implementation; source-only/synthetic prelabel; audit;
no-overwrite lock and exact seal; ordinal-0 activation and `LOCKED_READY`; full
Census A/B; classification/collision/selection; mapping A/B; native; GPU;
correctness; noise; decision; reproduction; still-fresh verification; truthful
report; exact terminal commit and postcommit audit.
