# S10 Stage 1 Entry Gate — Hard Artifact Blocker Memo

## 1. 結論

S10在任何tracked implementation、effective lock或outcome-bearing GPU work開始前，
已由read-only discovery直接確認`FT-BLOCKED-ACCESS`：

- 找不到符合本研究條件的actual generated `gfx942`、non-StreamK Ductile YAML；
- 因而也沒有可共同核對的exact generation command、cwd、authoritative input bytes、
  generator revision與byte-identical rerun provenance；
- source code、test fixture、failed fixture run、Python bytecode、手工重建或不相符的
  `gfx950` YAML都不能替代actual artifact。

這滿足parent §2.8的`frozen YAML provenance不足` hard stop。S10的terminal
criterion是`S1_ENTRY_BLOCKED`，scientific outcome為`blocked`，沒有outgoing edge。
S11為`not_activated`；S12、S13、S20及其他downstream checkpoints仍受gate約束。

本memo是預註冊的唯一early hard-blocker target。沒有建立S10 formal report，也沒有
建立S10 source、schema、contract、input、manifest、lock、evidence或decision artifact。

## 2. Authority 與執行邊界

- Baseline／S00 closure commit：
  `8d1be2af3e5033ee74a1a8e3b31536f132a014cb`
- S00 verified edge：`S00_EVIDENCE_READY -> S10`
- S10 Plan-A SHA-256：
  `696f6c6d680b3a82678d05ec18654647235263ff297799898dc87402f47eedd5`
- Frozen S10 Plan-B SHA-256：
  `4bd3f05cbfee9ab6031406230a3517a62f6ac63dd2a9c347cbad6bbf498f5ca8`
- Pinned GEKO ref：
  `refs/remotes/origin/users/pkamd/geko_pr`
- Pinned GEKO commit：
  `d32abacfd13579d1f523f035b7a10b0734c4ac47`
- Existing GPU container：`perlee`
- Push、dependency install、persistent container mutation、其他container、
  external-system write與whitelist expansion均未授權，也未執行。

Plan-B的early hard-blocker delivery恰為本memo、parent experiment plan、active
README與S10 design四個paths。Complete-evidence branch的13個implementation paths與
S10 formal report全數不在本次delivery。

## 3. Read-only discovery 與 direct observations

### 3.1 Workspace／known output roots

下列scope依序被檢查：

- `projects/hipblaslt/`
- active與prior `agent_run/`中的GEKO／Ductile workflow records
- `study_docs/research/ductile-origami-warmstart/`
- `projects/hipblaslt/build/`
- `projects/hipblaslt/tensilelite/build_tmp/`
- ignored `projects/hipblaslt/utilities/geko/`
- existing `perlee` container中的對應mount、build/cache roots與相關`/tmp`範圍
- pinned GEKO Git tree與直接相關tracked test/default inputs

成功的marker searches針對YAML/YML與generation records搜尋：

```text
Backend.Name / Name: Ductile
This yaml is auto-generated by geko.config_generator
scripts/config_generator.py
--gemm-log-path
geko.config_generator
```

直接相關local build/cache/container scopes沒有actual generated Ductile YAML或其成功
generation record。一次跨全部2,577 refs／reflog的病態唯讀掃描在約十分鐘後由Main
只終止該組exact bash/git processes；它沒有產生candidate，也不作為negative proof。
即使只找到detached YAML blob，缺少authoritative command/input/source lineage仍不
符合H1。

### 3.2 唯一具體gfx942 generator record

prior ignored verification record保存的命令是：

```bash
env PYTHONPATH=. PYTHONDONTWRITEBYTECODE=1 python3 \
  scripts/config_generator.py --hipblaslt ../.. \
  --config tests/config_generator/fixtures/minimal_config.yaml \
  --arch gfx942 \
  --outputPath <ignored-verification-output> \
  --no-shell-scripts --verbose 0
```

Direct result：

- exit：`1`
- error：`ImportError: cannot import name 'rocIsa' from 'rocisa'`
- generated output files：`0`
- record SHA-256：
  `dc13d95c59dac6ad37f73d5c61f5d2bb8cce04833f5b62be1082ccec068b7c9e`
- stderr SHA-256：
  `e5b329027da5ff87bc966c6f71fb143d7ea9a77aa4ae9d021b0d5655e0e46f49`

該input位於`tests/`，raw SHA-256為
`8d96f1d273c1791cd42fdca651e24c5a022e102f487f2e784ec2eac9bbef3623`，
且直接宣告`GA: false`、`StreamK: true`與只有兩個sizes。即使命令成功，它也不是
本研究要求的actual non-StreamK Ductile workflow artifact。

### 3.3 Pinned source 與不相符候選

Pinned GEKO objects可驗並包含generator、input normalizer、writer、
`_build_ductile`、ordered weights與Ductile backend group expansion；因此H2 source
identity為`PASS`。但「source有能力生成」不證明actual workflow曾以何input／command
生成。

同一Git object graph中的tracked Ductile test YAML都位於
`Tensile/Tests/common/ductile/gfx950/`。它們標示`gfx950`並明確skip `gfx942`；
也沒有與本次actual workflow相符的exact generation command/input/rerun record。
它們不會被改名或複製成S10 input。

Ignored GEKO directory只有`.pyc` cache，沒有對應source checkout；bytecode沒有被
import或用作provenance。

## 4. H1–H4 gate matrix

| Gate | 判定 | Direct basis | 影響 |
| --- | --- | --- | --- |
| H1 actual YAML/provenance | `FAIL` | 無actual bytes＋authoritative command/input/revision/rerun；唯一gfx942 attempt失敗且是錯誤test regime | `FT-BLOCKED-ACCESS` hard stop |
| H2 pinned GEKO source | `PASS` | commit與required source blobs可由Git objects核對；ignored cache排除 | 只證source identity，不能補H1 |
| H3 canonical mapping | `UNKNOWN / EVIDENCE_REQUIRED` | source chain plausible，但無actual candidate、same-lineage post-codegen library、effective-GSU runtime與完整Formocast parity record | 不捏造mapping PASS，也不額外宣稱mapping terminal failure |
| H4 gfx942/time budget | `NOT_EVALUATED / EVIDENCE_REQUIRED` | local使用授權與8張gfx942 visibility存在；無exact UTC window、enforceable collision control或dated ≥5 complete-work-day calendar | 不捏造availability PASS，也不把未找到record誤稱已證明denial |

H1單獨已足以觸發parent §2.8。H3／H4的未完成不會被改寫成額外已證明的failure，
也不會被用來延後或掩蓋H1。

## 5. H4 authority adjudication

兩名fresh `gpt-5.6-sol/xhigh` reviewers依使用者standing statement完成獨立立場、
一輪完整交互詰問與同一candidate consensus確認，最後均明確`AGREE`：

- 使用者已授權S10在既有`perlee`、exact-whitelist、no-install、no-mutation、
  no-push邊界內選定並使用一張exact `gfx942`；不需再詢問milestone-local permission。
- 此authorization不等於scheduled availability或dated ≥5-day budget。
- 若未來重新啟動S10，pre-lock raw allocation／schedule evidence必須先存在於
  authoritative source或ignored discovery，再由既有tracked manifest／lock lifecycle
  投影與fresh revalidation；不得新增第14個implementation path或循環自證。

因此本次blocking reason不是「等待使用者授權」。它是H1 actual artifact/provenance
已直接失敗。

## 6. Claim boundary 與未執行工作

本次證據只支持：

- preregistered input/provenance gate沒有可用actual artifact；
- S10必須fail closed；
- downstream不得啟動。

本次不支持：

- Formocast mapping好壞、ranking quality或factorization；
- Ductile Gen0效果；
- actual smoke/correctness/noise outcome；
- gfx942一般可用性；
- production／deployment readiness。

沒有執行：

- S10 unit/schema/preflight；
- lock或input sealing；
- generate／compile／benchmark／correctness；
- noise pilot；
- model ranking、treatment labels、guidance、D5 pool或Gen0 proposals；
- S11、S12、S13、S20及其他downstream work。

## 7. Recovery interface

要重新啟動S10而不降低原claim，至少需要先提供或建立：

1. actual generated `gfx942`、non-StreamK Ductile YAML exact bytes與source location；
2. exact generation command、cwd與authoritative input bytes/hashes；
3. generator commit與relevant Git blob identities；
4. 可直接解析的architecture、dtype/layout/transpose、same-space sizes、groups、
   candidate order與existing weights；
5. 若以rerun證provenance，byte-identical output；
6. H3所需same-lineage post-codegen/runtime mapping evidence；
7. H4所需exact device/window/collision controls與dated ≥5-day calendar。

取得新evidence後必須從S10重新做fresh planning、implementation與independent
verification；不得在本次blocked record上補寫假lock或outcome。任何recovery也不會
自行授權push。

## 8. Lifecycle record

- Implementation discovery：`EARLY_HARD_STOP / NO_TRACKED_IMPLEMENTATION`
- Selected terminal branch：exact 4-path early hard-blocker delivery
- Completion correctness：`FULL`
- Technical verification：`PASS`
- Verifier report SHA-256：
  `58e200acaf724be3be6d14b4ecb3e618a7047de8b7d9ef03bd56241bfd471ed0`
- Verifier verdict SHA-256：
  `da02aedce3774e33b59a612cd0818054cdd53f755ab6b78c38e1ed37febf58c6`
- Scientific outcome：`blocked`
- Criterion：`S1_ENTRY_BLOCKED`
- Lock：`absent`
- S10 formal report：`absent`
- Outgoing edge：none
- S11：`not_activated`
- Checkpoint completion：不宣稱；durable `BLOCKED`不是
  `CHECKPOINT_COMPLETE`

Fresh verifier已獨立重查actual-YAML/provenance headline、exact 4-path scope、
absence of outcome implementation/formal report/downstream work，以及本memo與三份
authority projection的一致性，判定completion correctness為`FULL`、scientific
outcome為`blocked`。這只進入blocked-branch closeout準備；原verifier仍須對本次
post-verification lifecycle投影做targeted recheck並另行簽發`CLOSEOUT_ACK`。在
exact-path commit與post-commit audit前，不宣稱durable terminal record已固定。
